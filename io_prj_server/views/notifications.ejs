<% if(notifications.length> 0) {
    notifications.forEach(n => { %>
    <div class="notification">
        <div class="notification-text">
            <%= n.message %>
        </div>
        <div>
            <button class="more"><a href="/project/id/<%= n.from_project_id %>">Więcej...</a></button>
        </div>
    </div>
    <% })} else { %>
        <div class="notification">
            <div class="notification-text">
                Brak powiadomień
            </div>
        </div>
        <% } %>